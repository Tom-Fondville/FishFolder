<local:UserControlNotifyPropertyChanged x:Class="FishFolder.View.UserControls.ProjectCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FishFolder.View.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="LightGray">
        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="10"  
                   Text="{Binding ProjectId }"/>
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10"
                   Text="{Binding ProjectName }"/>
        
        
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10"
                   Text="{Binding Project.ProjectName }"/>
    </Grid>
</local:UserControlNotifyPropertyChanged>
